<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationdrawerappasgmail">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mypic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mypic"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationDrawerAppAsGmail">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.NavigationDrawerAppAsGmail.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
